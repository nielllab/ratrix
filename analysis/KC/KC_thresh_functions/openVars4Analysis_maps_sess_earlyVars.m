% want to prompt user to open all files/variables needed for analysis
% this function needs vars created by the behavior session, dfof Movie 
% & early analysis (ball data output, maps files, early vars)

% the function loads the files

function f = fact(n)
    f = prod(1:n);
end