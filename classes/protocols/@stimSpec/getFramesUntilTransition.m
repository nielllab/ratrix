function fr=getFramesUntilTransition(spec)
% stimSpec.framesUntilTransition accessor function
% returns the number of frames until this phase transitions associated with the given stimSpec object

fr = spec.framesUntilTransition;