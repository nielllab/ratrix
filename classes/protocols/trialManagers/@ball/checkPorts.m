function out = checkPorts(tm,targetPorts,distractorPorts)

if ~(isempty(targetPorts) && isempty(distractorPorts))
    error('targetPorts and distractorPorts must both be empty for ball');
end

out=true;

end