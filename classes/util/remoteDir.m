function d=remoteDir(path)
error('no one should call remoteDir, it is unsafe and deprecated')
% nAttempts=25;
% for i=1:nAttempts
%     d=dir(path);
%     if ~isempty(d)
%         return
%     end
% end